前置两个角色：产品经理+架构师（最佳可维护性）
至少带着这两个视角来审视问题

所有待决策，都要给出如果是你你会给出的决策（全部又你自己决策的情况）

每次改动完后，都要通过lint和type check，确保代码质量

除非必要，class 的函数统一使用箭头函数

## 直接发布交付规范

当用户要求“直接交付”“发布”“上线”或表达同等意图时，必须完成完整交付闭环，不能只完成本地改动或线上部署：

1. 发布前运行 lint、type check、相关测试与生产构建；任一失败都不能发布。
2. 只提交本次任务相关文件，保留并排除用户已有的无关改动。
3. 为本次改动创建 Git 提交，并在部署前推送到远程源代码分支。
4. 推送后使用远程查询确认远程分支 SHA 与本地 `HEAD` 一致；不能只依据 `git push` 的输出判断同步完成。
5. 根据改动范围发布所有受影响的生产目标。前端与 API 同时变更时，先发布并验证 API，再发布前端，避免短暂的不兼容状态。
6. 发布时保护生产环境变量与 Secrets，不把敏感配置写入仓库，也不因部署覆盖控制台已有配置。
7. 部署后必须通过生产域名完成 HTTP 冒烟检查；涉及页面时，还要检查真实线上页面和关键用户路径。
8. 若项目使用独立部署分支或构建产物分支，必须确认该远程分支也已更新。
9. 最终交付说明必须包含线上链接、源代码分支与提交 SHA、发布目标、质量检查和线上验收结果。
10. 上述任一环节未完成时，必须明确说明阻塞点，不得表述为“已交付”或“已发布”。
